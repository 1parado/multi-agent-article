# 2026-09-03

- 将昨日的导航修复 + TOC 组件 + AGENT.md 规范提交并推送到 origin/main（commit 5412a27，9 files changed）。`.workbuddy/` 未纳入版本控制。
- 新增 3 篇论文摘要页（与现有 8 篇不重复、主题互补）：#09 OI-MAS（2601.04861，置信感知多尺度模型路由）、#10 Symphony-Coord（2602.00966，去中心化 bandit 涌现协调）、#11 HEART（2609.01736，自然语言工具原语 + 2.5 万函数仓库）。均基于 arXiv abs 页 + HTML 正文核验撰写。
- 同步更新：index.html（条目/横向对比/阅读顺序/8→11 篇）、MAS² 页加「下一篇：OI-MAS」、AGENT.md 顺序表、README（表格/仓库结构/篇数）。
- 新页面遵循 AGENT.md 规范：相对链接（无本地 PDF 用 arxiv pdf）、nav 导航链、TOC 组件。全站自检通过（0 绝对链接、11 页全有 toc-btn、链路与顺序表一致）。已本地提交 1084d3a，未推送。
- 应用户要求推送 1084d3a 到 origin/main，远端已同步 11 篇论文的完整站点。
- 前端美化 v2（用户从 10 个方向中勾选 8 项）：①分类色签（7 类专属色 --cat + .dot，eyebrow/idx 着色）②index hero SVG 编排流水线插画 + 论文页 arXiv 水印 .wm ③顶部阅读进度条 #pbar ④TOC scroll-spy 当前章节高亮 ⑤IBM Plex Mono webfont + tldr 首字下沉 ⑥index 论文条目 hover 浮起红边 ⑦滚动显现（IntersectionObserver，尊重 reduced-motion）⑧条形图入场生长动画。全部 12 页通过批量脚本（.workbuddy/beautify_v2.py + beautify_patch.py）注入，脚本 v2 替换旧 TOC 脚本（注意旧 marker 在 style 块也出现，须用 "var heads" JS 定位；MoRe/SoK/MAS² eyebrow 无 PAPER 前缀）。AGENT.md 已重写为 v2 规范。已提交 f2c10bc 未推送。
- 用户反馈「不需要首字变大」：已从 12 个页面 + AGENT.md 移除 .tldr::first-letter 首字下沉规则（commit 58a10fe，未推送）。
- 推送 f2c10bc + 58a10fe 到 origin/main，美化 v2 全量上线远端。
- 前端美化 v3（用户从第二轮 10 个方向中勾选 6 项 + 自定义 1 项）：①h2 标题锚点链接（悬停显示 #，点击复制直链，Clipboard API + execCommand 降级，移动端隐藏）②返回顶部按钮 #totop（滚动 >600px 出现，TOC 面板上移避让）③键盘 ←/→ 翻页（靠 .nav 链接文案中 ←/→ 字符识别）④h2 章节编号 .secno ⑤index 对比表升级（斑马纹/hover/首列 mono 红字）⑥移动端 @media(max-width:640px) 专项适配 ⑦GitHub 图标链接（论文页 .links 行 + index footer，指向 github.com/1parado/multi-agent-article）。通过 .workbuddy/beautify_v3.py + gh_footer_patch.py 注入 12 页（注意：index footer 注入检查若在 V3_STYLE 注入后判断 "ghlink" not in s 会因 CSS 字样误判已存在，须分离 body 后检查）。AGENT.md 重写为 v3 规范。全站核验通过（12 页均含 v3 增强脚本、ghlink、标签配平）。已提交 98f423f（13 files, +1138/−162），未推送。
- 推送 98f423f 到 origin/main（远端 ls-remote 核实指向 98f423f）。注意本机沙箱里 refs/remotes/origin/* 引用不持久，`git status` 显示 "[gone]" 仅为本地显示问题，推送/拉取均正常。
- 前端美化 v4（产品/阅读体验轮：从 PM 视角给出的 10+ 方向中用户勾选 7 项）：
  - ①术语表：新建 glossary.html，31 词条按五组划分（基础/编排/路由效率/在线决策/安全工具），词条自带 id；论文页正文术语首次出现处用 `.gloss` 点线样式包装，跳 glossary.html#id
  - ②index 分类筛选/检索：#fltbar 顶部 chips + #q 文本框，.paper 卡片加 data-cat（7 类），纯 DOM 过滤无跳转；data-cat/颜色统一脱胎自 beautify_v4.py 的 PAPER/CAT_COLOR 元数据表
  - ③引用一键复制：每篇论文页 .links 后插入 .citebar（BibTeX/APA 双键）+ 隐藏 #cite-json（每页元数据合成），点击复制到剪贴板
  - ④打印/导出 PDF：v4 样式含 @media print 适配（白底黑字、隐藏交互、分页控制），可配 Edge headless --print-to-pdf
  - ⑤无障碍/字体韧性：字体改为 `<link rel="preload" as="style" onload>` 异步 + `<noscript>` 回退，替换旧同步 webfont link
  - ⑥分享预览+站身份：各页 head 注入 og:/twitter: meta（title/desc/url/image）+ favicon.svg（64×64 红底白点 1→N planner 示意）；index 无本地图，og:image 用 favicon
  - ⑦About+页脚完善：新建 about.html（收录标准/阅读提示/MIT/时间表/维护者+GitHub）；全站 footer 换成 .sitefoot 双行超集（首页/术语表/关于/反馈/© 行内 GitHub 链接）；glossary/about 特殊页右侧 nav 不加 →
  - 注入编排：.workbuddy/beautify_v4.py（PAPER/TERM_LINKS/ALIASES/CAT_COLOR 元数据 + assert 未含 v4 幂等）；术语嫁接用「掩码法」先把 h2/h3 与完整标签替换成空白再定位最后术语首现，避免污染标题/已有 <a>；.workbuddy/v4_scan.py 术语预检
  - 踩坑与修复：CITEBAR_HTML 多出一个 </div> 造成游离闭合 → v4_fix_straydiv.py 逐页清理 fixed 1；自检假阳性（<li→<link、<p→<line）→ 改用 `<div\b`/`<p\b`/`<li\b` 精确正则；glossary/about 首轮缺 v4 script 已手工补；cite-json 仅对论文页判定
  - README 死链根因修复：MoRe/SoK/MAS² 三篇 PDF 误写仓库 raw/main 路径而本地无文件 → 统一改 arXiv https://arxiv.org/pdf/<id>.pdf 外链；年份措辞改「arXiv 2025-09 ~ 2026-09」避免 MAS²(2509) 歧义
  - AGENT.md 重写为 v4（新增 glossary/about/favicon 归属、组件引用表含原文片段、30+ 词条清单、8 步自检表）；全站 14 页（index + 11 论文 + glossary + about）注入后 14/14 PASS 精确自检
  - 状态：全部落盘未提交，待用户「推送」指令后 git add -A + commit（建议信息含 "Polish v4: glossary+about, filter/search, one-click cite, print CSS, async fonts, OG/favicon, site footers"）→ push origin/main
- 用户下达「推送」：git add -A 提交 8c3be90（28 files，+4209/−163，含 .workbuddy/ 下 beautify 系列脚本与日志一并入库）→ push origin/main 成功（98f423f..8c3be90），ls-remote 核实远端 main 指向 8c3be90。v4 全量上线。
- 用户要求以「吹毛求疵的产品人 + 艺术家」双视角点评站点。核验结论（有据）：①全站 12 页 og:image 均为 hero.svg（880×380，比例 2.32:1、无每页区分）②index 无 URL 筛选状态（仅 heading anchor 用 replaceState）③无 JSON-LD/canonical/sitemap/robots/theme-color/apple-touch-icon ④论文页无 arXiv 编号/日期/收录时间戳 ⑤仓库无 LICENSE 且 about 页无任何许可措辞 ⑥已做且做得好：筛选空态 #fltnone、lang=zh-CN、约 880×380。艺术家方向拟了 ~12 条（期刊封面生成、index 星图、动态 hero、页边注、ghost 章号、目录卡式引用、已读藏书章、术语图鉴、colophon 版权页、统一图元 404 等）。待用户在 AskUserQuestion 中勾选后落地。
- 前端美化 v5（用户从双轮点评中勾选 8 项）：
  - ①期刊封面：.workbuddy/gen_covers.py（PIL 程序化生成），输出 covers/ 14 张 1200×630 PNG（11 论文 + index/glossary/about）+ apple-touch-icon.png 180×180（红底 planner→3 agents）。设计：双层红框 + 14px 纸本横纹底纹 + 右上 240px 幽灵编号 + eyebrow "PAPER DIGEST · VOL.1 · Nº n" + 中文主标题 + mono 英文副标 + 右侧确定性拓扑点线（arXiv 号为随机种子 → 9 节点 / index 11 节点） + 底部 ARXIV/URL/类别色签（仅论文页）。
  - ②每页专属 og:image：beautify_v5.py 把 og:image 从 hero.svg 替换为 covers/<本页>.png，新增 og:image:width(1200)/height(630) + twitter:image（同步）；glossary/about 用 covers/glossary.png/about.png。
  - ③head 站身份：theme-color #A02C2C + apple-touch-icon.png + canonical 绝对 URL + JSON-LD（论文 ScholarlyArticle 带 headline/url/datePublished/dateModified/isPartOf/keywords/publisher；index WebSite；glossary/about WebPage）。
  - ④论文页 .pubmeta（arXiv 编号/投稿年月/[venue]/中文版收录 2026-09-03）+ .epigraph（一句话导读，红左边线 blockquote，引用 "— 本站导读"，文在 EPIGRAPH 字典）。
  - ⑤论文页 .mnotes 旁注栏：4 张卡片 — 馆藏信息（arXiv/投稿/收录）、分类（颜色点 + 链 glossary.html#<cat>）、核心术语（TERM_LINKS 首位 + 计数）、阅读顺序（上一篇/本篇 Nº/下一篇，红字本篇）。body.page-paper + .wrap 内 paper-main 双栏（grid 1fr 232px，gap 48px，sticky top:36px），≤1240px 单列。
  - ⑥index .viewbar 视图切换 + 已读藏书章：列表/画廊/星图三视图 + 提示 + 已读数 #vhint-r；URL ?cat=&q=&view= 同步（URLSearchParams + replaceState）；gallery 网格用 covers PNG、星图 SVG 11 节点 sine 路径 + 类别色 hover 浮层；localStorage 键 mpa-read，论文页滚动 ≥85% 或 30s 记为已读，index 卡片右上角红底 .readmark 印章 + "#vhint-r 已读 n / 11"；打印隐藏所有 v5 增强。
  - ⑦LICENSE：新建双段式许可 — 原创 CC BY-NC 4.0 + 第三方论文版权声明 + 数据透明说明；about.html 同步措辞。
  - ⑧SEO/404：新建 sitemap.xml（14 条 URL 含 lastmod/priority）、robots.txt（Sitemap 引用）、404.html（红框 404 印章 SVG + 回首页/术语表/关于/GitHub 链接）。
- v5 注入踩坑与修复（值得记）：
  - **og:image 双条 bug**：12 页（index + 11 篇论文）og:image 数=2。根因：v4 模板原本就有 og:image（hero.svg），v5 注入时（1）向 meta 块追加了新的 og:image+width+height 三连对 + twitter:image（2）又对原 og:image 做 re.sub 替换 → 两条并存。**首次修复方向错了**（先删了"紧邻 width"的那条即 meta 块里的 → 留下上方位 v4 单条，与 width 断开）；**改删"不紧邻 width"的那条** → 反而删光了所有 og:image → 在 width 前补回单条。最终正确做法写回 beautify_v5.py：把原 re.sub 改成「删除任何不紧邻 width 的 og:image」（count=1），并在主循环末尾加 assert `og:image == 1` / `twitter:image == 1` 守护回归。
  - AGENT.md §4.2 明文规定"og:image 三连对必须相邻且全页唯一"，并入 §8 自检，教训沉淀给后续 Agent。
- AGENT.md 升级到 v5：① §0 文件清单增 covers/、404.html、LICENSE、robots.txt、sitemap.xml、apple-touch-icon.png；hero.svg 移除 og:image 角色 ② §2 新增论文必做步骤增 4-6 三步（ORDER_ARX/EPIGRAPH 登记、gen_covers 重跑、sitemap 追加） ③ §3 组件历史补 v5 行、样式块 D 与脚本 v5 ④ §4.2 重写为 v5 完整 head 模板 ⑤ §4.9-4.11 三节为新：论文页四元素、index 视图与藏书章、SEO/许可/站身份文件 ⑥ §6 词条数 30→31 修正 ⑧ §7 脚本表增 beautify_v5.py/gen_covers.py 行 ⑨ §8 自检补 og/twitter/canonical/JSON-LD/paper-paper/paper-main/mnotes/pubmeta/epigraph/viewbar/gallery/star/covers/sitemap/404 各项 + 浏览器实测补画廊/星图切换、URL 视图保持、藏书章显示、分享带专属封面；§9 同步清单由 5 项扩到 8 项。
- 全站 14 页自检：**ALL 14/14 PASS**（theme-color×1、apple-touch×1、canonical×1、ld+json×1、og:image=1 且三连对、twitter:image=1、v5 CSS、v5 script、论文页 page-paper/paper-main/mnotes/pubmeta/epigraph/wrap2col/mnote4、index viewbar+gallery+star+cards11、glossary/about own-cover）。
- 状态：v5 全量落盘未提交。**等用户「推送」指令**，建议 commit 信息含 "Polish v5: covers, per-page og/canonical/JSON-LD, marginalia/epigraph/pubmeta, index views, read-mark, 404/LICENSE/robots/sitemap"。
- 前端美化 v6（用户请求：①集成 giscus 让访客对每个 HTML 发表评论 + 「选中即批注」WPS 式交互 ②TOC 从右下移到左上角）：
  - 仓库侧：确认 1parado/multi-agent-article 存在且有 admin 权限；`gh api -X PATCH ... -f has_discussions=true` 启用 Discussions；默认分类 General 的 CATEGORY_ID=DIC_kwDOUK42-M4DExUU、REPO_ID=R_kgDOUK42-A（实测）。**评论映射用 og:title（全站 14 页唯一）而非页面 URL**——本地 / 预览 / GitHub Pages 路径不同，og:title 保证同一评论串处处可复用。新论文加入时必须保证 og:title 唯一，否则评论串会与别页混串。
  - 注入脚本 .workbuddy/beautify_v6.py（幂等：断言 v6 标记未存在）：每页 `</head>` 前加 v6 `<style>`、`</body>` 前加 `.giscwrap#giscus` 容器 + v6 懒加载脚本（IntersectionObserver rootMargin 1400px 近视口才注入 giscus.app/client.js）。
  - TOC 左移：v6 CSS 把 #toc-btn 改 fixed left:14px top:14px、#toc-panel 改 top:66px left:14px 下拉；@media(min-width:1040px) 用 opacity/pointer-events !important 压过 v1「滚动>180px 才显示」→ 桌面常显，移动端仍滚动后出现；#totop 仍在右下不动。
  - 批注交互：mouseup 命中 .wrap 内选区（≥4 字）→ 浮动 .annbar（✎ 写批注 / 复制引用）→ .annmask 弹层展示被引原文（300 字截断）+ 出处（所在章节+页 og:title，**须在 mouseup 时即缓存 annHead**，弹层打开时选区已折叠取不到 nearestHead）→ textarea 批注 → 一键复制「Markdown 引用块+出处+批注+via 落款」到剪贴板并滚到 .giscwrap 闪烁提示，用户自行粘贴到 giscus 输入框发布。键盘 ←/→ 有选区或工具条显示时 preventDefault 拦截（v3 翻页冲突）；Esc 关弹层。
  - 踩坑：脚本路径拼接双重前缀致 OSError → 改 os.path.basename；从页面抽取的 v6 JS 用 node --check 全过；Edge headless 运行时验证在本机输出为空已放弃，改静态核验。
  - AGENT.md 升级到 v6：§0/§3/§7 补 v6 行；新增 §4.12（目录左上+桌面常显）、§4.13（giscus 容器/配置常量统一维护在 beautify_v6.py 顶部 GISCUS_BLOCK、App 需一次授权）、§4.14（批注交互规则与限制）；§8/§9 同步。
  - 全站 14 页自检 **14/14 PASS**（注入编号、.giscwrap 容器、v6 script、样式块配平）。
  - **待办**：①等用户「推送」指令（当前工作区同时含未提交的 v5 与 v6，建议分两个 commit 或合一个，由用户定）；②提醒用户到 github.com/apps/giscus 安装一次 giscus App（管理员权限）后评论区即生效，无需改代码。
- 推送 v5+v6 单 commit `f06eef0` 后，用户反馈：论文页旁注栏「馆藏信息」卡片（arXiv/投稿/中文版收录）与正文 `.pubmeta` 行完全重复 → 要求移除。已用 `.workbuddy/patches/rm_archive_card.py` 对 11 个论文页幂等移除（`<div class="mnote"><h4>馆藏信息</h4>…</div>` 整卡），每页 4→3 张（留 分类/核心术语/阅读顺序）。同步改 `.workbuddy/beautify_v5.py`（`paper_extras` 里 `notes_html` 去掉馆藏信息片段，源码与页面一致，重跑不再生成）+ AGENT.md（§4.9 改 3 张含溯源注释；§8 自检改「恰好 3 张、不得含馆藏信息」；§7 脚本表登记补丁）。全站复核：无残留（grep 命中蹾 beautify_v5.py 注释系溯源语句）、`.pubmeta` 完好、aside 平衡、页面 diff 均为 −1 行。
- 用户「推送」：commit `f3ec16e`（"Remove redundant archive-info card from paper marginalia (dup of .pubmeta); keep 3 cards, sync beautify_v5.py + AGENT.md"，15 files +37/−17）→ push origin/main 成功（f06eef0..f3ec16e），ls-remote 核实远端指向 f3ec16e。
- 用户进一步反馈：右侧剩的 分类 / 核心术语 / 阅读顺序 三张卡片（连同 `.paper-main` 双栏）整个都不需要——分类已在正文 meta 行可见、核心术语用 `.gloss` 点线引用、阅读顺序在 footer 的 ←/→ 与 index 横向对比表都已承载。已写 `.workbuddy/patches/rm_mnotes.py` 幂等移除 `<aside class="mnotes">…</aside>` 整段 + 解开 `<div class="paper-main">…</div>` 双栏包裹（11 篇 4→0 张卡片、单栏回归）。再写 `.workbuddy/patches/rm_mnotes_css.py` 同步清掉 v5 CSS 里 5 条失效 `.mnotes` 样式、`.paper-main` 单行、grid 双栏声明、窄屏 @media 与 print 兜底（每页 mnotes=4→0、paper-main=1→0）。同步改 `.workbuddy/beautify_v5.py`：`paper_extras` 不再返 notes_html、`paper-main` 包裹逻辑注释掉（保留回滚说明）、CSS 删双栏 + 卡片样式、自检去掉 `paper-main/mnotes` 计数；AGENT.md §0/§2/§3/§4.9/§7/§8 全面同步（v5 后续：旁注栏 + 双栏移除说明）。复跑 beautify_v5.py 14/14 PASS、无 paper-main/mnotes。**落盘未提交**，建议 commit "Remove paper marginalia (aside.mnotes + .paper-main dual-col); back to single-column paper body"。
