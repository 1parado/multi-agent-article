# Reading & Design Polish

Applied via `scripts/apply-ux-all.py` on `index.html`.

## Reading

- Higher contrast secondary text
- Tighter measure (`720px`) and body line-height `1.85`
- First-screen content visible (reveal only below fold)
- Skip link to paper list
- Immersion chip (section + exit)
- URL sync for view / immersive

## Design

- Link color hierarchy (`--link`)
- Paper hover shadow + read state
- Gallery rest shadow
- Softer watermark
- Viewbar separated from filters; view mode hints
- Print hides chrome; reduced-motion respected
